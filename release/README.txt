To get started, follow these steps:

1. Copy config.example.yaml and rename it to config.yaml to use as a template.
2. Create an OAuth application by visiting the settings section of your osu! profile.
3. Run downloader.exe to begin the program.